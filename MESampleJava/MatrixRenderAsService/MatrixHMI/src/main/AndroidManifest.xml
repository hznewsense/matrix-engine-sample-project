<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <queries>
        <package android:name="com.ns.matrix.sample.service" />
    </queries>
    <application
        android:name=".MatrixClientApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo_hmi"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_hmi"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppSampleSuperClient">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden"
            android:screenOrientation="landscape"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppSampleSuperClient.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>